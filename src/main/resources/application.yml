server.port: 8079
spring:
  datasource:
    url: jdbc:postgresql://localhost:54321/postgres?currentSchema=public
    username: ecarrefour
    password: ecarrefour
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
server:
  error:
    include-stacktrace: always
